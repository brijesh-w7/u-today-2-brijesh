public enum IngredientType
{
    Vegetable,
    Cheese,
    Meat
}

public enum IngredientState
{
    Raw,
    Processed   // chopped (vegetable) or cooked (meat)
}

public enum GameState
{
    Menu,
    Playing,
    Paused,
    GameOver
}
